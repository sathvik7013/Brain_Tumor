services:
  - type: web
    name: brain-tumor-backend
    env: python
    rootDir: brain-tumor-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: CORS_ORIGINS
        value: '*'